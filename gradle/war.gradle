apply plugin: "war"

bootWar {
    mainClassName = "com.hosyfy.erp.ErpApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
